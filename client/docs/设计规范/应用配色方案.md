# 应用配色方案

## 🎨 设计理念

基于 UI/UX Pro Max 专业建议，采用 SaaS 应用标准配色方案，强调信任感和专业性。

## 📊 核心配色

### 主色调（Primary）
- **颜色**: `#2563EB` (blue-600)
- **用途**: 激活状态、主要操作按钮、品牌标识
- **Tailwind**: `bg-blue-600`, `text-blue-600`, `border-blue-600`

### 次要色（Secondary）
- **颜色**: `#3B82F6` (blue-500)
- **用途**: 悬停状态、次要强调
- **Tailwind**: `bg-blue-500`, `text-blue-500`

### 强调色（Accent）
- **颜色**: `#10B981` (emerald-600)
- **用途**: 成功状态、积极反馈
- **Tailwind**: `bg-emerald-600`, `text-emerald-600`

### 背景色（Background）
- **主背景**: `#F8FAFC` (slate-50)
- **卡片背景**: `#FFFFFF` (white)
- **次要背景**: `#F1F5F9` (slate-100)
- **Tailwind**: `bg-slate-50`, `bg-white`, `bg-slate-100`

### 文字色（Text）
- **主文字**: `#1E293B` (slate-800)
- **次要文字**: `#64748B` (slate-500)
- **禁用文字**: `#CBD5E1` (slate-300)
- **Tailwind**: `text-slate-800`, `text-slate-500`, `text-slate-300`

### 边框色（Border）
- **主边框**: `#E2E8F0` (slate-200)
- **强调边框**: `#2563EB` (blue-600)
- **Tailwind**: `border-slate-200`, `border-blue-600`

## 🎯 组件配色规范

### Tabs 组件
```tsx
// TabsList 背景
bg-slate-100  // 浅灰色背景，不会太深

// TabsTrigger 默认状态
text-slate-600 hover:text-slate-900

// TabsTrigger 激活状态
bg-white text-blue-600 shadow-sm  // 白色背景 + 蓝色文字 + 轻微阴影
```

### 导航菜单
```tsx
// 激活状态
bg-blue-50 text-blue-600 border-l-4 border-blue-600

// 默认状态
text-slate-600 hover:bg-slate-50

// 禁用状态
text-slate-300 cursor-not-allowed
```

### 用户头像
```tsx
// 头像背景
bg-blue-600  // 使用主色调

// 图标颜色
text-white
```

### 卡片组件
```tsx
// 卡片背景
bg-white rounded-lg shadow-sm border border-slate-200

// 卡片内容
p-6 space-y-4
```

### 按钮组件
```tsx
// 主要按钮
bg-blue-600 text-white hover:bg-blue-700

// 次要按钮
bg-slate-100 text-slate-700 hover:bg-slate-200

// 危险按钮
bg-red-600 text-white hover:bg-red-700
```

## 📱 状态指示色

### 成功状态
- **颜色**: `#10B981` (emerald-600)
- **用途**: 成功提示、完成状态
- **示例**: 订阅有效、操作成功

### 警告状态
- **颜色**: `#F59E0B` (amber-500)
- **用途**: 警告提示、注意事项
- **示例**: 即将到期、需要注意

### 错误状态
- **颜色**: `#EF4444` (red-500)
- **用途**: 错误提示、失败状态
- **示例**: 订阅过期、操作失败

### 信息状态
- **颜色**: `#3B82F6` (blue-500)
- **用途**: 信息提示、中性反馈
- **示例**: 提示信息、帮助文本

## 🎨 语义化配色

### 存储使用显示
```tsx
// 已使用空间（主要信息）
bg-blue-100 text-blue-600

// 文件数量（积极信息）
bg-emerald-100 text-emerald-600

// 最后更新（次要信息）
bg-slate-100 text-slate-600
```

### 订阅状态
```tsx
// 有效订阅
text-green-500 bg-green-50

// 过期订阅
text-red-500 bg-red-50
```

## ✨ 交互状态

### 悬停状态（Hover）
- 背景变化：`hover:bg-slate-50`
- 文字变化：`hover:text-slate-900`
- 阴影变化：`hover:shadow-md`
- 过渡动画：`transition-colors duration-200`

### 激活状态（Active）
- 背景高亮：`bg-blue-50`
- 文字高亮：`text-blue-600`
- 边框强调：`border-l-4 border-blue-600`
- 阴影效果：`shadow-sm`

### 焦点状态（Focus）
- 轮廓显示：`focus-visible:ring-2 focus-visible:ring-blue-500`
- 边框高亮：`focus-visible:border-blue-600`

### 禁用状态（Disabled）
- 文字变灰：`disabled:text-slate-300`
- 背景变灰：`disabled:bg-slate-100`
- 禁用指针：`disabled:cursor-not-allowed`
- 透明度降低：`disabled:opacity-50`

## 🌓 深色模式（未来扩展）

当前应用使用浅色模式，未来可扩展深色模式：

```tsx
// 背景色
dark:bg-slate-900

// 文字色
dark:text-slate-100

// 边框色
dark:border-slate-700

// 卡片背景
dark:bg-slate-800
```

## 📐 设计原则

### 1. 对比度
- 确保文字与背景对比度至少 4.5:1
- 重要信息使用高对比度（slate-900）
- 次要信息使用中对比度（slate-600）

### 2. 层次感
- 使用阴影区分层级：`shadow-sm`, `shadow-md`, `shadow-lg`
- 使用背景色区分区域：`bg-white`, `bg-slate-50`
- 使用边框分隔内容：`border border-slate-200`

### 3. 一致性
- 所有激活状态使用蓝色（blue-600）
- 所有成功状态使用绿色（emerald-600）
- 所有错误状态使用红色（red-500）

### 4. 可访问性
- 所有交互元素有明确的视觉反馈
- 颜色不是唯一的信息传达方式
- 支持键盘导航和焦点状态

## 🎯 实施检查清单

- [x] Tabs 组件使用浅色背景（slate-100）
- [x] 激活状态使用蓝色（blue-600）
- [x] 导航菜单激活状态使用蓝色
- [x] 用户头像使用品牌色（blue-600）
- [x] 存储卡片使用语义化颜色
- [x] 所有交互元素有悬停反馈
- [x] 文字对比度符合 WCAG 标准
- [x] 边框清晰可见（slate-200）

## 📚 参考资源

- **UI/UX Pro Max**: SaaS 产品配色建议
- **Tailwind CSS**: 官方颜色系统
- **WCAG 2.1**: 可访问性标准
- **Material Design**: 交互状态规范

---

**最后更新**: 2026-02-01
**设计系统版本**: 1.0
